spring:
  application:
    name: springcloud-demo-security-oauth2-jwt
  
server:
  context-path: /
  port: 8761

management:
  context-path: /admin

security:
  user:
    name: user
    password: p@ssw1rd
  oauth2:
    resource:
      filter-order: 3
      